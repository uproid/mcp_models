name: mcp_models
description: >
  Dart model classes for the Model Context Protocol (MCP).
  Covers all JSON-RPC messages, tools, resources, prompts, sampling,
  elicitation, tasks, and notifications defined in the official schema.
version: 1.0.0
repository: https://github.com/uproid/mcp_models
homepage: https://github.com/uproid/mcp_models
issue_tracker: https://github.com/uproid/mcp_models/issues

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies: {}

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
