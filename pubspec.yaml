name: mcp_models
description: Dart model classes for the Model Context Protocol (MCP) — tools, resources, prompts, sampling, tasks and notifications. No codegen.
version: 1.0.1
repository: https://github.com/uproid/mcp_models
homepage: https://github.com/uproid/mcp_models
issue_tracker: https://github.com/uproid/mcp_models/issues

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies: {}

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
